// Função contador de 0 a 100

// Imprimindo todos os impares

// Se ao passar os parametros a ordem for inversa corrija isso

function contadorImpar(min = 0, max = 100) {
    while(min < max) {
        if(min > max) {
            let maior = (max + min) - max // (100 + 200) - 100 = 200
            let menor = (min + max) - min // (200 + 10) - 200 = 150
        }
        
    }
}

contadorImpar(320, 80)